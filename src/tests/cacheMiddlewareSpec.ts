import cacheMiddleware from '../utilities/cacheMiddleware';

describe('test caching a requested url', () => {
  it('is going to be implemented in the future, not mandatory for the project', () => {
    expect(typeof cacheMiddleware).toBe('function');
  });
});
